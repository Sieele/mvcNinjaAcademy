@model IEnumerable<mvcAppNinjaDemo.Models.OverviewTeamCount>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TeamName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NinjaCount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HasMission)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RankMission)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TeamName)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NinjaCount)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.HasMission)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.RankMission)
                </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TeamId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TeamId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TeamId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
